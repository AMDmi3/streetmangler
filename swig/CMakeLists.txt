FIND_PACKAGE(SWIG REQUIRED)
INCLUDE(${SWIG_USE_FILE})

FIND_PACKAGE(PythonLibs)
INCLUDE_DIRECTORIES(${PYTHON_INCLUDE_PATH})

INCLUDE_DIRECTORIES(${CMAKE_CURRENT_SOURCE_DIR})
INCLUDE_DIRECTORIES(${PROJECT_SOURCE_DIR}/lib ${PROJECT_SOURCE_DIR}/contrib/tspell)
LINK_DIRECTORIES(${PROJECT_SOURCE_DIR})

SET(CMAKE_SWIG_FLAGS "")

SET_SOURCE_FILES_PROPERTIES(_streetmangler.i PROPERTIES CPLUSPLUS ON)
#SET_SOURCE_FILES_PROPERTIES(_streetmangler.i PROPERTIES SWIG_FLAGS "-includeall")
SWIG_ADD_MODULE(streetmangler python _streetmangler.i)
SWIG_LINK_LIBRARIES(streetmangler ${PYTHON_LIBRARIES} -lstreetmangler)
